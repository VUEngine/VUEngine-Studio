{%- set events = project.items.GameConfig.project.events -%}
{%- import 'templates/partials/macros.njk' as macros -%}
{% include('templates/partials/fileHeader/fileHeader.h.njk') %}

#ifndef GAME_EVENTS_H_
#define GAME_EVENTS_H_

{{ macros.sectionHeader('Includes') }}

#include <Events.h>

{{ macros.sectionHeader('Definitions') }}

{% if events | length > 0 -%}
enum GameEvents
{
	{%- for event in events | values | removeEmpty | sort %}
	kEvent{{ event | sanitizeSpecName }}{% if loop.first %} = kEventGameFirst + 1{% endif %},
	{%- endfor %}
};
{%- endif %}

#endif
