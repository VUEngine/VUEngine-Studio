import React from 'react';
import VContainer from '../../../../../core/browser/components/VContainer';

interface CollisionsProps {
}

export default function Collisions(props: CollisionsProps): JSX.Element {
    // const { pattern, currentNote } = props;

    return <VContainer gap={10}>
        Collisions
    </VContainer>;
}
