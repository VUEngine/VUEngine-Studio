import React from 'react';

interface TrackerProps {
}

export default function Tracker(props: TrackerProps): React.JSX.Element {
    const {

    } = props;

    return (
        <div style={{ margin: 'calc(var(--theia-ui-padding) * 2)' }}>
            Tracker mode not yet implemented.
        </div>
    );
}
